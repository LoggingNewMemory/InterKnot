name: interknot
description: "Unified Proxy Network"
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  android_alarm_manager_plus: ^3.0.1
  flutter_inappwebview: ^6.0.0
  just_audio: ^0.9.34
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
